import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DeepTalks app smoke test', (WidgetTester tester) async {
    // Placeholder test – Firebase must be mocked for widget tests
    expect(1 + 1, equals(2));
  });
}
